# MiniGame Platform 🎮

Веб-портал классических мини-игр с акцентом на чистый интерфейс, высокую производительность и полную автономность (Offline-first PWA).

Деплоится на Vercel как статическое/клиентское приложение без необходимости настройки внешних баз данных или переменных окружения.

---

## Особенности

* ⚡ **100% Offline-first:** Полная автономность — играйте без интернета благодаря Service Worker (PWA).
* 💾 **Локальные рекорды:** Все результаты, победы, лучшее время и очки сохраняются локально в браузере (`localStorage`).
* 🎨 **Темы оформления:** Поддержка светлой и тёмной тем с адаптивным дизайном под мобильные устройства и десктоп.
* 🧩 **Модульная архитектура:** Чистое разделение игровой логики (чистый TypeScript) и слоя отображения (React/Next.js).
* 🚀 **Нулевой бэкенд:** Не требует внешних сервисов, баз данных или API-ключей.

---

## Текущий статус игр

| Игра | Категория | Статус | Описание |
|---|---|---|---|
| **Судоку** | Логика | ✅ Готово | 4 уровня сложности (Лёгкий, Средний, Сложный, Эксперт), умные подсказки, режим заметок, валидация ошибок, таймер, отмена/повтор ходов |
| **Сапёр** | Логика | ⏳ В планах | Классический сапёр с генерацией гарантированно решаемых полей |
| **2048** | Аркада | ⏳ В планах | Классическая плиточная головоломка с плавными анимациями свайпов |
| **Змейка** | Аркада | ⏳ В планах | Олдскульная змейка с управлением жестами и клавиатурой |
| **Шахматы** | Настольные | ⏳ В планах | Игра против локального движка Stockfish (WASM в Web Worker) |

---

## Стек технологий

* **Фреймворк:** [Next.js 16](https://nextjs.org/) (App Router, Turbopack)
* **Интерфейс:** React 19, TypeScript, [Tailwind CSS](https://tailwindcss.com/)
* **Компоненты:** [Lucide React](https://lucide.dev/), Tailwind CSS, Framer Motion
* **Состояние:** [Zustand](https://github.com/pmndrs/zustand)
* **PWA / Оффлайн:** Service Worker + LocalStorage API

---

## Структура проекта

```
minigame/
├── app/                  # Next.js App Router (страницы и лэйауты)
│   ├── (games)/          # Маршруты конкретных игр (/sudoku)
│   ├── leaderboard/      # Таблица личных рекордов и история игр
│   ├── profile/          # Профиль локального игрока и статистика
│   └── layout.tsx        # Корневой шаблон и провайдеры
├── components/           # React-компоненты
│   ├── auth/             # Меню и профиль игрока
│   ├── games/            # Общие компоненты игр и оверлеи
│   │   └── sudoku/       # Сетка, контролы и нумпад для Судоку
│   ├── layout/           # Шапка, подвал, переключатель тем
│   ├── shared/           # Селекторы сложности, таймер, табло
│   └── ui/               # Базовые UI-примитивы (кнопки и т.д.)
├── games/                # Изолированные движки игр (чистый TypeScript)
│   ├── _lib/             # PRNG, базовые интерфейсы GameEngine
│   ├── registry.ts       # Единый реестр игр
│   └── sudoku/           # Генератор, решатель и валидатор Судоку
├── lib/                  # Утилиты и адаптеры
│   ├── pwa/              # Регистрация Service Worker и статус сети
│   └── storage/          # Менеджер локальных рекордов и статистики
├── public/               # Манифест, Service Worker, иконки
└── stores/               # Zustand хранилища (auth, theme, settings)
```

---

## Архитектура: как устроены игры

Каждая игра строится по строгому интерфейсу **`GameEngine<TState, TAction>`** в папке `games/<game_id>/`:
1. **Логика независима от React**: движок, генератор и валидатор написаны на чистом TypeScript.
2. **Предсказуемое состояние**: состояние изменяется через чистые функции `applyAction(state, action)`.
3. **Хук-обёртка**: React-слой подключается через хук `use<Game>()` (например, `useSudoku()`), управляющий таймером, историей ходов (Undo/Redo) и состоянием оверлея.
4. **Сохранение результатов**: при завершении партии результат автоматически фиксируется в `lib/storage/records.ts`.

---

## Запуск и разработка

```bash
# 1. Установка зависимостей
npm install

# 2. Запуск локального сервера разработки
npm run dev
```

Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000)

```bash
# 3. Проверка типов и линтинг
npm run lint

# 4. Продакшен-сборка
npm run build
```

---

## Деплой

Проект на 100% готов к деплою на **Vercel** или любой другой статический хостинг.  
**Никаких переменных окружения (`.env`) указывать не требуется.**
